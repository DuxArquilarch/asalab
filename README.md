all code here is made for fun and to experiences 

V8.6 new layout + mode to calculate the constants basead in Oswall law and reynolds

V9.5 new table that includes more calculations Weight needed to fly, stall curve recalculated, max cl, max polar, cl produced by airfoil, CD by reynolds numbers

Version XYZ Experimental :
1. Multi-Profile Aerodynamic DatabaseThe code includes a lookup and interpolation system for NACA 4-digit and 5-digit airfoils (e.g., NACA 4412, 23012).Dynamic Interpolation: It performs linear interpolation of aerodynamic coefficients ($a_0$, $\alpha_{L0}$, $C_{d0}$) based on the calculated Reynolds Number ($Re$).Geometric Generation: Uses analytical thickness and camber formulas (White §8.1) to reconstruct the physical coordinates of the airfoils for visualization.2. Wing Planform Analysis (AsaT)The simulator handles three distinct wing geometries, applying specific physical corrections for each:Rectangular: Standard Oswald efficiency calculations.Elliptical: Models the "ideal" lift distribution where induced drag is minimized ($e = 1.0$).Delta: Implements a Vortex Lattice Method (VLM) approximation for low aspect ratio ($AR$) wings, blending linear lifting line theory with delta-specific slope corrections (White §8.8).3. Advanced Aerodynamic CorrectionsThe simulation doesn't just use "static" values; it adjusts for real-world flight conditions:Compressibility: Uses the Prandtl-Glauert Correction ($1/\sqrt{1 - Ma^2}$) to account for air density changes as the flow approaches Mach 0.7.Finite Wing Theory: Converts 2D airfoil lift slopes into 3D wing lift slopes ($C_{L\alpha}$) using the Prandtl Lifting Line Theory, accounting for $AR$ and the Oswald efficiency factor.Boundary Layer Transition: Predicts the laminar-to-turbulent transition point using Michel’s Criterion based on the local Reynolds number.4. Flow Visualization (Biot-Savart)The script includes a 2D flow field simulator. It uses the Biot-Savart Law to calculate induced velocities around the airfoil, treating the lifting surface as a point vortex to visualize how the air is deflected (downwash) based on the required lift ($C_L$).## Suggested Resume DescriptionAerodynamic Simulation Engine (AsalabXYZ) | Python, NumPy, PlotlyDeveloped a wing performance simulator based on Frank M. White’s fluid mechanics principles, supporting NACA 4/5-digit airfoils and multiple wing planforms (Rectangular, Elliptical, Delta).Implemented a Reynolds-dependent interpolation system for aerodynamic coefficients and applied Prandtl-Glauert compressibility corrections for high-subsonic Mach numbers.Modeled 3D induced drag and lift-slope variations using Prandtl’s Lifting Line Theory and empirical Oswald efficiency factors.Created an interactive 5-panel dashboard using Plotly to visualize Lift/Drag polars, stall angles, and airflow streamlines via Biot-Savart induced velocity calculations.
